export interface Product {
  brand: string;
  name: string;
  img: string;
}

export interface PageProp {
  withHeaderFooter: boolean;
}

export interface Review {
  authorName: string;
  img: string;
  text: string;
}
